# sayelf-birdpick · 冷启动商业模式与真实训练数据方案

> 状态：试验设计（Hypothesis） · 日期：2026-09-09
> 当前独立 HTML 是本地产品演示，不包含账号、支付、云端上传或模型训练后台。

## 结论先行

sayelf-birdpick 的护城河不应是“更便宜的批量选片”，而应是：

**本地隐私信任 → 用户先获得选片价值 → 用户自愿贡献可追溯标注 → 人工复核形成高质量数据 → 模型持续变好 → 用户得到更好的选片结果。**

商业收入来自本地额度包和可控的云端增值服务；训练数据来自明确授权的贡献计划，二者必须分开说明、分开记录、分开撤回。

## 1. 冷启动方案

### 1.1 首发用户规则（试验假设）

- 前 100 名完成登记并激活的用户，获得激活后 30 天免费体验。
- 体验期最多处理 1,000 张照片，全部优先在用户本地电脑完成。
- 不要求用户贡献照片或标注；拒绝贡献不影响试用、购买或导出。
- 体验结束后，继续保留基础免费额度；需要更多处理量时购买本地额度包。
- 名额、激活时间、已用张数和到期时间必须由正式账号服务端记录。独立 HTML 只能展示规则，不能防止重复注册或实现真实计时。

### 1.2 体验结束后的价格

以下是当前产品约定的本地额度包，不自动续费：

| 档位 | 额度 | 价格 | 处理位置 |
|---|---:|---:|---|
| 免费基础 | 50 张 | ¥0 | 用户本地电脑 |
| 加量包 | 50 张 | ¥9.9 | 用户本地电脑 |
| 小批量 | 100 张 | ¥19.9 | 用户本地电脑 |
| 批量包 | 1,000 张 | ¥99 | 用户本地电脑 |
| 大批量 | 10,000 张 | ¥490 | 用户本地电脑 |

建议第一版把额度包设计为一次购买、无自动扣费，并在支付页面明确有效期、退款条件和“本地处理不等于云端模型调用”。“免费基础 50 张”与“付费 50 张”可以并存，但必须分别叫“每月免费额度”和“50 张加量包”，否则用户会以为重复收费。当前 HTML 仍按一次会话演示，正式版再改为按账号累计扣除额度。

### 1.3 收费模式意见

我的建议是先保留你给出的价格，先验证“用户是否愿意为节省时间付费”，不要同时改价格、模型和数据规则：

1. **基础免费**：正式版建议每月 50 张本地处理，永久保留，用于持续获客和复访；当前 HTML 只演示一次会话额度。
2. **一次性加量包**：50 张 ¥9.9、100 张 ¥19.9、1,000 张 ¥99、10,000 张 ¥490；名称用“加量包”，额度按账号累计，先设置 12 个月有效，不做自动续费。
3. **订阅暂缓**：当有至少 20 个真实付费用户后，再测试月度订阅或工作室版；在此之前，订阅会增加退款、续费和支持复杂度。
4. **云端单独计费**：本地包不包含云端调用；云端采用预充值或月度预算双限额，避免用户以为 ¥99 本地包包含无限 API。
5. **贡献积分不是现金**：积分只能兑换本地额度或明确的服务权益，不折现、不承诺收益分成；每月设置兑换上限，防止刷量和成本失控。

公开同类产品的定价形态已经覆盖“免费试用 + 订阅/按项目”的组合：[Narrative Select 定价页](https://narrative.so/select/pricing/) 的 Pro 页面显示为每月 15 美元、按年计费；[FilterPixel 定价说明](https://filterpixel.com/blog/filterpixel-pricing) 则把基础筛选做成免费项目，并按月度档位和项目量销售。它们说明用户愿意为“节省选片时间、批量处理和工作流整合”付费，但不证明 sayelf-birdpick 必须照搬美元订阅价。当前低价本地额度包更适合先做中国鸟类摄影社群的冷启动，后续是否增加订阅应由付费留存和支持成本决定。

### 1.4 云端模型增值服务

本地能力解决隐私和基础成本；只有用户主动需要语义识别、复杂复核或大模型解释时才打开云端能力。

- 默认关闭，按月设置“费用上限 + API 调用上限”双重闸门。
- 任一上限先达到就停止云端调用，并在界面显示已用量和原因。
- 不在浏览器 `localStorage` 保存长期 API 密钥；正式版使用本地安全存储或服务端密钥代理。
- 云端请求只发送完成必要任务的最小字段；原图、EXIF、GPS 和人像等敏感信息默认不发送。
- 当前 HTML 只保存演示用控制参数，不发起 API 请求，也不计费。

## 2. 获客与宣传

### 2.1 一句话定位

**拍鸟的人，不必先把照片上传云端，也能先把一批照片挑出结果；愿意帮助模型变好的人，再用清楚、可撤回的方式贡献训练候选数据。**

### 2.2 内容主线

宣传不是先讲模型名词，而是先让目标用户看到“我今天就能省时间”：

1. **痛点钩子**：拍了 1,000 张，真正值得后期的有多少张？
2. **即时演示**：打开页面 → 拖入照片 → 保留 / 待定 / 排除 → 导出标记。
3. **信任证据**：默认本地处理；页面明确不调用 RF-DETR、不上传图片、不包含模型权重。
4. **数据邀请**：用户已经看见价值后，再展示训练数据贡献卡，并说明授权范围、用途和退出方式。
5. **行动入口**：首批 100 名、30 天免费、最多 1,000 张；先体验，不要求授权数据。

### 2.3 首发内容试验

以下是内容试验，不是任何平台的流量保证。每条内容只验证一个问题，并记录来源、点击、首次导入和复访：

| 内容 | 画面/文案 | 验证指标 |
|---|---|---|
| 20 秒短演示 | “不上传照片，先把废片筛掉” | 3 秒留存、落地页点击 |
| 前后对比 | 一批连拍从混乱到三类结果 | 首次导入率、完成一次导出率 |
| 隐私说明 | 浏览器本地处理、云端默认关闭 | 隐私疑问率、试用激活率 |
| 真实标注故事 | 展示“筛选信号 ≠ 训练真值” | 授权阅读率、有效标注率 |
| 用户案例 | 用户节省的整理时间和导出结果 | 复访率、推荐率、付费转化 |

推荐先在鸟类摄影社群、自然观察社群和短视频/图文渠道做小批量测试；不要购买无法追踪来源的泛流量。每一条内容使用同一个入口和邀请码，才能知道哪种宣传真的带来有效用户。

### 2.4 首发话术

> 拍了很多鸟片，却不想把原图先传到云端？打开 sayelf-birdpick，拖入一批照片，先在本机按“保留 / 待定 / 排除”整理。首批 100 名激活用户，30 天内最多免费处理 1,000 张。训练数据贡献完全自愿，先体验价值，再决定是否帮助模型变好。

## 3. 真实训练数据如何获得

### 3.1 先区分三种东西

- **用户筛选信号**：当前页面的保留、待定、排除。它有价值，但噪声较大，不能直接当作训练真值。
- **训练候选数据**：用户明确授权后导出的照片索引和标记，还要经过隐私检查与人工复核。
- **训练真值数据**：至少包含任务所需的规范标注，例如鸟类类别、检测框、关键点、清晰度或难例标签，并记录复核者和一致性。

当前页面的“生成本地训练候选清单”只导出第三种之前的候选清单，不上传图片、不开始训练。这样可以真实展示数据闭环入口，但不虚构后台能力。

### 3.2 最小可行数据闭环

1. **本地采集**：用户主动导入照片；默认只在浏览器或本地应用内处理。
2. **明确授权**：单独勾选训练用途和照片处理权确认；记录授权时间、政策版本和授权范围。
3. **本地脱敏**：正式上传前移除 EXIF/GPS，检查人脸、车牌、未成年人和第三方照片；原图路径不进入清单。
4. **候选标注**：保存用户筛选标签，并引导用户补充鸟类类别、框、眼部/关键点、清晰度和难例说明。
5. **质量复核**：对关键样本采用双人复核或专家抽检，保留一致率、争议原因和最终标签；未复核的数据只能进入候选集。
6. **受控入库**：用户再次确认后上传到私有存储。原图、标注、授权记录和审计日志分开保存，使用不可逆样本哈希与匿名贡献者 ID 关联。
7. **训练与评估**：按数据集版本训练候选模型，使用固定且不参与训练的评估集，记录 precision、recall、漏检难例和版本差异。
8. **回馈用户**：向贡献者提供数据回执：数据集版本、接收数量、通过复核数量、模型版本和评估摘要；不把“参与过训练”包装成个人模型一定提升。

成熟的标注工具可以作为独立插件接入（例如 CVAT 或同类工具），不应把复杂标注系统塞进首个独立 HTML。

### 3.3 用户和产品方分别能拿到什么

用户应能随时导出自己的标注清单，并在正式后台看到贡献回执、数据集版本和模型改进摘要。产品方只有在得到明确授权、满足隐私和保留规则后，才能持有训练候选原图及其标注。

模型训练后，单个用户通常能拿到的是自己照片的标注/推理结果、模型版本和评估摘要；是否提供模型权重、原始数据副本或收益分成，必须在贡献条款中单独写清楚，不能默示。已经进入训练并改变模型参数的数据，也要在授权文本中说明撤回的边界和处理方式。

## 4. 护城河设计

护城河按形成难度排序：

- **高质量难例库**：逆光、远距离、遮挡、飞行、枝叶干扰等真实鸟类场景，比泛化的公开图片更贴近用户。
- **可追溯标注资产**：每个样本有来源授权、标注版本、复核记录和模型使用记录，后来者难以低成本复制。
- **主动学习闭环**：模型把最不确定的样本交给用户复核，优先收集最能提升模型的难例，而不是盲目收集数量。
- **本地工作流信任**：用户可以先获得价值，不必先交出原图；这是数据贡献的前置条件。
- **社群分发网络**：摄影师、观鸟者、自然教育者形成案例和推荐网络，降低长期获客成本。

单纯收集“用户勾选了多少次”不构成护城河。真正的资产是：**授权清楚、标签准确、难例集中、版本可复现、模型提升可测量**。

## 5. 指标与停止规则

首批 100 名用户建议只观察一组核心指标，以下数值是试验门槛，不是市场事实：

- 激活率：落地页到首次导入 ≥ 60%。
- 首次价值时间：从打开到完成一批三类筛选 ≤ 3 分钟。
- 7 日复访率 ≥ 25%。
- 训练授权率：用户完成一次有效筛选后再测，目标 ≥ 20%。
- 有效标注率：抽检后可进入训练集的候选样本 ≥ 60%。
- 付费转化率：体验到期或额度用尽后的目标 ≥ 8%。
- 云端成本：任何单用户月度成本不得突破其双限额；超限自动停用。

如果用户看不懂三类筛选、首次价值时间超过 3 分钟，先改产品和教程；不要急着扩大投放。若授权率高但有效标注率低，优先改标注任务和复核流程，而不是收集更多未经复核的照片。

## 6. 90 天实施顺序

### 第 0 阶段：现在

- 保持独立 HTML、本地优先、明确许可证与权重边界。
- 展示首批 100 名 / 30 天 / 1,000 张规则。
- 通过明确勾选生成本地训练候选清单。
- 记录试验指标，不上传真实照片。

### 第 1 阶段：首批用户验证

- 使用邀请码或登记表管理 100 个名额和激活时间。
- 收集匿名的产品事件：首次导入、完成筛选、导出、复访、付费意向。
- 由用户自己决定是否把候选清单交给产品方；先收 20–30 个深度访谈样本。

### 第 2 阶段：受控数据后台

- 加入账号、授权版本、逐批撤回、数据删除请求、私有对象存储和审计日志。
- 接入成熟标注工具，建立双人复核和固定评估集。
- 发布第一份数据集卡片和模型卡片，说明来源、范围、限制和指标。

### 第 3 阶段：模型与收入闭环

- 训练第一版鸟类检测/关键点模型，只对已通过质量门槛的数据训练。
- 将模型版本和结果回馈到本地工作流，比较升级前后真实用户指标。
- 上线额度包支付和云端双限额；不默认开启云端，不把云端费用混入本地套餐。

## 7. 本轮明确不做

- 不在没有账号服务的 HTML 里假装实现“前 100 名”或真实 30 天计时。
- 不默认上传原图，不在用户不知情时把筛选结果用于训练。
- 不把三类人工筛选直接宣称为 AI 训练真值。
- 不在当前版本接入支付、云端 API、自动训练或模型权重下载。
- 不承诺模型效果、流量爆发或贡献者自动获得模型所有权。
